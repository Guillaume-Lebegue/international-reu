{
  "$schema": "https://deno.land/x/denon@2.4.7/schema.json",
  "scripts": {
    "start": {
      "cmd": "deno run --allow-env --allow-read --allow-net app.ts",
      "desc": "Start my app.ts file"
    }
  }
}
